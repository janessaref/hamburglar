<li>
	{{burger_name}}

	<button type="button" class="btn btn-danger" class="eatBurger" data-id="{{id}}" data-yumBurger="{{devoured}}">
		{{#if devoured}}EAT ME{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">THROW BURGER AWAY</button> --}}
</li>

